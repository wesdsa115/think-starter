{
	// Worker, client, and Node code need different libs/types, so each is its own project.
	"files": [],
	"references": [
		{ "path": "./tsconfig.worker.json" },
		{ "path": "./tsconfig.client.json" },
		{ "path": "./tsconfig.node.json" }
	]
}
